#include <bits/stdc++.h>
using namespace std;

string s;
int t;

int main () {
    scanf ("%d", &t);
    for (int i = 1; i <= t; ++i) {
        cin >>s;
        if (s.find("114514") != s.npos) puts("AAAAAA");
        else puts("Abuchulaile");
    }
    return 0;
}