#include<bits/stdc++.h>
using namespace std;
int n,m,T;
double a[N],b[N];
void solve()
{
	scanf("%d%d", &n, &m);
	for(int i = 0; i < n; i ++)
		scanf("%lf", &a[i]);
	double Z = 0;
	double l = 0, r = 1e15;
	while(R - L > 1e-10)
	{
		a[]
	}
	printf("%.20lf\n", -Z * Z);
}
int main()
{
	scanf("%d",&T);
	for(int i = 1; i <= T; i ++)
	{
		printf("Case %d: ",i);	
		solve();
	}
	return 0;
}
