#include <bits/stdc++.h>
using namespace std;

const int N = 10;
int n, s[N], b[N];

int main () {
    scanf ("%d", &n);
    for (int i = 1; i <= n; ++i) scanf ("%d%d", &s[i], &b[i]);
    
    return 0;
}